import React from 'react';

const Header = () => (
  <header style={{ padding: '1rem 2rem', background: '#0f172a', color: '#fff', display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
    <h2 style={{ margin: 0, fontSize: '1.25rem' }}>Expense Tracker & Budget Manager</h2>
  </header>
);

export default Header;
